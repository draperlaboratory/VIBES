{
  "max-tries": 3,
  "wp-params": {
    "func": "main",
    "postcond" : "(assert true)"
  },
  "patch-space" : [
    {"offset" : "0x3c8",
     "size" : "24"}
  ],
  "patches" : [
    {"patch-name" : "math",
     "patch-code" : "int input, output, five, three, x;
                     five = 5;
                     x = input * five - 3;
                     three = 3;
                     output = x * three;",
     "patch-point" : "0x103ee",
     "patch-size" : 4,
     "patch-vars" : [
       {"name": "input",
        "at-entry": {
          "stored-in": "register",
          "register": "R0"
        }
       },
       {"name": "output",
        "at-exit": {
          "stored-in": "register",
          "register": "R0"
        }
       }
     ],
     "extra-constraints": "
       % exclude callee-save registers from the solution
       constraint exclude_reg('R4');
       constraint exclude_reg('R5');
       constraint exclude_reg('R6');
       constraint exclude_reg('R7');
       constraint exclude_reg('R8');
       constraint exclude_reg('R9');
       constraint exclude_reg('R10');
       constraint exclude_reg('R11');
       constraint exclude_reg('SP');
     "
    }
  ]
}
