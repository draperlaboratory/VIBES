{
  "max-tries": 1,
  "wp-params": {
    "func": "main",
    "postcond" : "(assert (= (bvadd R1_mod #x00000002) R0_orig))"
  },
  "patches" : [
    {"patch-name" : "ret-3",
     "patch-code" : "int retvar, tmp; retvar = 3; tmp = retvar;",
     "patch-point" : "0x103c8",
     "patch-size" : 32,
     "patch-vars": [
       {"name": "retvar",
        "at-exit": {
          "stored-in": "register",
	      "register": "R0"
        }
       },
       {"name": "tmp",
        "at-exit": {
          "stored-in": "register",
	      "register": "R1"
        }
       }
     ]
    }
  ]
}
